// Default Pages configuration for Next
const nextConfig = { 
    images: { 
        unoptimized: true 
    }, 
    experimental: { images: { unoptimized: true } }, 
    output: 'export',
    basePath: "/styleMoE" 
}
module.exports = nextConfig